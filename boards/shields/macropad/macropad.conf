# Enable USB HID (wired, plug-and-play, no drivers)
CONFIG_ZMK_USB=y

# Enable Bluetooth HID (wireless)
CONFIG_ZMK_BLE=y

# Store up to 5 Bluetooth device pairings
CONFIG_BT_MAX_CONN=5
CONFIG_BT_MAX_PAIRED=5

# Report battery level to host (if battery is connected)
CONFIG_ZMK_BATTERY_REPORTING=y
